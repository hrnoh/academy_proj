<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kpu.academy.mapper.UserMapper">

	<insert id="create">
		insert into user (id, pwd, name, grade, age, sex, email, address, phone, mPhone, status, role, relation, cno)
		values (#{id}, #{pwd}, #{name}, #{grade}, #{age}, #{sex}, #{email}, #{address}, #{phone}, #{mPhone}, #{status}, #{role}, #{relation}, #{cno})
	</insert>

	<select id="read" resultType="UserVO">
		select *
		from user
		where uno = #{uno}
	</select>
	
	<update id="update">
		update user set 
		name=#{name}, 
		grade=#{grade}, 
		age=#{age},
		sex=#{sex}, 
		email=#{email}, 
		address=#{address}, 
		phone=#{phone}, 
		mPhone=#{mPhone},
		status=#{status},
		role=#{role},
		relation=#{relation},
		cno=#{cno}
		where uno = #{uno}
	</update>
	
	<delete id="delete">
		delete from user where uno = #{uno}
	</delete>
	
	<select id="listAll" resultType="UserVO">
	<![CDATA[
		select *
		from user
		order by uno desc, regdate desc
	]]>
	</select>
	
	<!-- 학생 리스트 -->
	<select id="studentList" resultType="UserVO">
	<![CDATA[
		select *
		from user
		where role like 'student' OR role like 'parents'
		order by uno desc, regdate desc
	]]>
	</select>
	
	<!-- 강사 리스트 -->
	<select id="teacherList" resultType="UserVO">
	<![CDATA[
		select *
		from user
	]]>
		<if test="role != null">
		<if test="role == 'student'.toString()">
			where role like 'student' OR role like 'parents'
		</if>
		<if test="role == 'teacher'.toString()">
			where role like 'teacher'
		</if>
		</if>
	<![CDATA[
		order by uno desc, regdate desc
	]]>
	</select>
</mapper>